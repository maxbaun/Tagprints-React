export const initPageElements = () => {
	initFlickity();
};

function initFlickity() {
	const {Flickity} = window;
	// const sliders = Array.from(document.querySelectorAll('.js-flickity'));

	// sliders.forEach(slider => {
	// 	new Flickity(slider);
	// });

}
